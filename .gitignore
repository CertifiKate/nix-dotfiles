# Devenv
.devenv*
devenv.local.nix
devenv.local.yaml

# direnv
.direnv

.envrc

# pre-commit
.pre-commit-config.yaml

.terraform

# Terraform state files
# Incus allows for refreshing the state file, so we can ignore it in git
terraform.tfvars
.terraform/
*.tfstate*
.terraform.lock.hcl
*.env